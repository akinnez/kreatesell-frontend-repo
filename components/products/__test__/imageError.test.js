import renderer from "react-test-renderer";
import ImageError from "../ImageError";

// const mockProps = {
//   file: {
//     name: "mockFileName",
//   },
//   errors: ["mock - Error", "-", "mock - Error 2"],
//   deleteFile: jest.fn(),
// };
it("renders a snapshot of <ImageError />", () => {
  //   const imageError = renderer.create(<ImageError {...mockProps} />).toJSON();
  //   expect(imageError).toMatchSnapshot();
});
